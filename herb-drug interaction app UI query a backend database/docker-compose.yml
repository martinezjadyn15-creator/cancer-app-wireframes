services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: herb_drug_interactions
    ports:
      - "5432:5432"
    volumes:
      - herb_drug_pgdata:/var/lib/postgresql/data

volumes:
  herb_drug_pgdata:
